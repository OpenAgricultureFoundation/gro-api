language: python
python: 3.4
install:
    - pip install .
    - pip install coverage
    - pip install coveralls
script: coverage run --source=. --omit=env/*,*test*,*migrations* setup.py test
after_success: coveralls
