language: python
python: 3.4
install:
    - bash setup_environment.sh
    - pip install coverage
    - pip install coveralls
before_script: source .env
script:
    - python `which coverage`--source=. --omit=env,*test*,*migrations* manage.py test
    - coverage report -m
after-success: coveralls --verbose
